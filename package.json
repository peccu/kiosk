{
  "name": "kiosk",
  "version": "1.0.0",
  "description": "kiosk dashboard base template",
  "// main": "html/index.js",
  "source": "src/index.html",
  "type": "module",
  "scripts": {
    "// build": "tsc",
    "build": "npx parcel build src/*.html --dist-dir html",
    "format": "npx prettier . --write",
    "// dev": "npx serve html",
    "dev": "npx parcel src/*.html --dist-dir html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "peccu",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
